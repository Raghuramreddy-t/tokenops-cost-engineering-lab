# Portfolio Consumer Reference

This folder is documentation only. It is not the live app.

The portfolio repository should check out TokenOps during its deployment workflow, run `npm run build:embed`, copy `dist/portfolio-embed/*` to `assets/external/tokenops/`, and mount it in `pages/tokenops.html`.

Do not deploy TokenOps itself as a separate public website.
